"""
SpeedScan - requirements_new.txt
==================================
Dependências para as novas funcionalidades.
Instale com: pip install -r requirements_new.txt

Nota: As dependências marcadas com [OPCIONAL] são extras que ampliam funcionalidades.
O SpeedScan funciona com degradação graciosa sem elas.
"""

REQUIREMENTS_CONTENT = """
# =========================================
# SpeedScan - Novas Dependências (v1.0+)
# =========================================

# --- Monitoramento de Sistema (já no projeto) ---
psutil>=5.9.0

# --- Interface (já no projeto) ---
customtkinter>=5.2.0

# --- Temperatura de GPU NVIDIA ---
# [OPCIONAL] Para GPUs NVIDIA
pynvml>=11.0.0
GPUtil>=1.4.0

# --- S.M.A.R.T. (Saúde dos Discos) ---
# [OPCIONAL] Interface Python para smartctl
pySMART>=1.3.0

# --- Teste de Velocidade ---
speedtest-cli>=2.1.3

# --- Gráficos Históricos ---
matplotlib>=3.7.0

# --- Scanner de Rede ---
# [OPCIONAL] Scanner mais avançado (requer root/admin)
# python-nmap>=0.7.1

# --- Geração de PDF ---
# [OPCIONAL] Para relatórios em PDF
reportlab>=4.0.0
# Alternativa mais completa:
# weasyprint>=60.0

# --- Ícone na Bandeja (System Tray) ---
pystray>=0.19.0
Pillow>=10.0.0

# --- Requisições HTTP ---
# (urllib já vem na stdlib, mas requests é mais ergonômico)
requests>=2.31.0

# --- Windows específico ---
# [OPCIONAL] Para temperatura no Windows via WMI
# wmi>=1.5.1  (apenas Windows)
# pywin32>=306  (apenas Windows)

# --- macOS específico ---
# [OPCIONAL] osx-cpu-temp via Homebrew: brew install osx-cpu-temp

# =========================================
# Instalação rápida (apenas essenciais):
# pip install psutil customtkinter matplotlib pystray Pillow speedtest-cli
#
# Instalação completa:
# pip install psutil customtkinter matplotlib pystray Pillow speedtest-cli pynvml GPUtil pySMART reportlab requests
# =========================================
"""

print(REQUIREMENTS_CONTENT)
